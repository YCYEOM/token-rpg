# Token RPG

## 말투 및 톤앤매너

- "도움이 되셨길 바랍니다", "기억하세요", "놀랍게도" 같은 AI 특유의 상투적인 맺음말과
  과장된 수식어는 절대 금지한다.
- 군더더기 없는 담백한 구어체 또는 전문적인 문체(~합니다, ~입니다)를 사용하라.
- 피동태(~에 의해) 대신 능동태를 사용하고, 문장은 무조건 짧고 간결하게 끊어서 작성하라.

적용 범위: README·게임 내 한글 문구·릴리스 노트·커밋 메시지 등 사용자가 읽는 모든 한글 텍스트.

## 버저닝

버전은 `token_rpg.py` 의 `__version__` 한 줄이 전부다. `pyproject.toml`(hatch)·
`scripts/build-macos-app.sh`(Info.plist)·CLI·게임 화면이 모두 이 값을 읽어 간다.

`x.y.z` 를 올리는 기준:

| 자리 | 올리는 때 |
|---|---|
| `z` | **기본값.** 버그 수정, 밸런스 조정, 문구·문서 수정, 리팩터링, 기존 기능 개선 |
| `y` | 큰 기능이나 새 메뉴를 추가할 때 (예: 슬롯·혼 사냥·원정 같은 새 놀 거리, 새 탭) |
| `x` | 사용자가 명시적으로 지시할 때만. 스스로 올리지 않는다 |

애매하면 `z` 를 올린다. 저장 형식이 바뀌거나 호환성이 깨져도 그것만으로는 `y` 가 아니다 —
새 기능이나 새 메뉴가 들어갈 때만 `y` 다.

릴리스: `__version__` 수정 → 커밋 → `vX.Y.Z` 태그 → GitHub 릴리스 발행.
발행하면 `publish.yml` 이 `selftest` 를 돌리고 PyPI 에 올린다.
